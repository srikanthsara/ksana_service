apiVersion: v1
kind: Service

metadata:
  name: ksana-app
  namespace: ksana

  labels:
    app: ksana-app
    component: ksana-app

spec:

  selector:
    app: ksana-app

  ports:

    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

  type: NodePort