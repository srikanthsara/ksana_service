apiVersion: v1
kind: Service

metadata:
  name: grocerysearchservice
  namespace: ksana

spec:

  selector:
    app: grocerysearchservice

  ports:

    - port: 8762
      targetPort: 8762

  type: ClusterIP