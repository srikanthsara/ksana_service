apiVersion: v1
kind: Service

metadata:
  name: apigateway
  namespace: ksana

  labels:
    app: apigateway
    component: backend

spec:

  selector:
    app: apigateway

  ports:

    - name: http
      protocol: TCP
      port: 9090
      targetPort: 9090

  type: NodePort